name: Deploy to Azure

on:
  push:
    branches: [deploy_app_to_Azure]

env:
  REGISTRY: ${{ secrets.ACR_LOGIN_SERVER }}
  IMAGE_NAME: fews-agent
  RESOURCE_GROUP: FEWS_LLM

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Log in to Azure Container Registry
        run: echo "${{ secrets.ACR_PASSWORD }}" | docker login ${{ env.REGISTRY }} -u ${{ secrets.ACR_USERNAME }} --password-stdin

      - name: Build and push image
        run: |
          docker build -t $REGISTRY/$IMAGE_NAME:latest .
          docker push $REGISTRY/$IMAGE_NAME:latest

      # TODO: Uncomment once you have a service principal (AZURE_CREDENTIALS secret)
      # - name: Azure Login
      #   uses: azure/login@v2
      #   with:
      #     creds: ${{ secrets.AZURE_CREDENTIALS }}

      # - name: Deploy to App Service
      #   run: |
      #     az webapp config container set \
      #       --name "${{ secrets.WEB_APP_NAME }}" \
      #       --resource-group ${{ env.RESOURCE_GROUP }} \
      #       --container-image-name "${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest" \
      #       --container-registry-url "https://${{ env.REGISTRY }}" \
      #       --container-registry-user "${{ secrets.ACR_USERNAME }}" \
      #       --container-registry-password "${{ secrets.ACR_PASSWORD }}"

      #     az webapp restart \
      #       --name "${{ secrets.WEB_APP_NAME }}" \
      #       --resource-group ${{ env.RESOURCE_GROUP }}

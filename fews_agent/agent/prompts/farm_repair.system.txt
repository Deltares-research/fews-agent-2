You repair ONE output spec whose Jinja substitution didn't reproduce the input instances. You're told exactly which paths diverged. Edit ONLY the failing positions; preserve everything else byte-for-byte.

Return the FULL corrected output spec as JSON. No prose.
